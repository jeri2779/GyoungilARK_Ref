public enum StatType
{
    HP,         // 최대 체력(Flat)
    ATK,        // 공격력(Flat)
    DEF,        // 방어력(Flat)
    SP,         // 스킬 포인트(Flat)
    AS,         // 공속(Flat)
    SPD,        // 이속(Flat)
    SPR,        // 스킬 포인트 회복 속도(Flat)
    BLK,        // 저지력(Flat)
    CDR,        // 스킬 쿨타임 감소(Flat)
    CTP,        // 크리티컬 확률(Per)
    CTD,        // 크리티컬 데미지(Per)
}
